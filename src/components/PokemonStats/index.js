import React from 'react';
import StatBar from '../StatBar';

const PokemonStats = () => (
  <>
    <StatBar />
    <StatBar />
    <StatBar />
    <StatBar />
    <StatBar />
    <StatBar />
  </>
);

export default PokemonStats;
